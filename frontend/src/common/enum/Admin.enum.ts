export enum ELogsWindowLogType {
  INFO = "INFO",
  WARNING = "WARNING",
  ERROR = "ERROR",
  DEBUG = "DEBUG",
}

export enum EUsersLogsStatus {
  idle = "idle",
  processing = "processing",
  ready = "ready",
}

export enum EUserChatsRole {
  user = "user",
  assistant = "assistant",
}
